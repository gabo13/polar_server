#include <math.h>
//#include <iostream>
//#include <cassert>
//using namespace std;

//float x = 50;
//float y = 0;
//float l1 = 50;
//float l2 = 50;
//float d = 100; //a gép szélessége
//
//
//float getA(float x, float y) {
//	float a= sqrt( pow(x,2) + pow(y,2) );
//	return a;
//}
//
//float getB(float x, float y) {
//	float b= sqrt( pow(d-x,2) + pow(y,2) );
//	return b;
//}
//float getX(float l1, float l2) {
//	//float x = sqrt(pow(l1,2)-pow(get_y(l1, l2),2));
//    float x = (pow(d,2)+pow(l1,2)-pow(l2,2))/(2*d);
//    return x;
//}
//float getY(float l1, float l2) {
//	//float y= sqrt( (pow(l2,2)/(pow(d,2) - 2*d) ) - pow(l1,2) );
//	float y = sqrt(pow(l1,2)-pow(getX(l1,l2),2));
//    return y;
//}
